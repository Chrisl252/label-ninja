# DECISIONS_LOG.md — Label Ninja Decision Record

## 2026-09-01: Brick 3 — frontend export wiring (metered server exports, auth UI, paywall, drawer, ES module split)

- **Browser-print bypass REMOVED (the load-bearing decision):** all five `window.print()` flows (`printCurrentWorkspace`, `printEditorLabel`, `generateBinBatch`, `generateWhatnotBatch`, `printSingleFNSKU`) and the header "Print Labels" button are gone. Every tool's primary CTA is now "Download PDF" → builder → `POST /api/export` → blob download → optional "Open PDF" (blob URL in a new tab — the user prints from their downloaded artifact). Consequence: server metering is authoritative; nothing renderable bypasses the 10-use counter. The `@media print` CSS stays (inert), `#batch-print-area` and the `afterprint` handler were deleted, and the dead pdfmake + Tesseract.js CDN tags dropped (JsBarcode stays for client preview, PapaParse for B7). Ctrl+P now prints the dark app — intentional; printing routes through the downloaded exact-size PDF.
- **ES module split (13 modules under `public/js/app/`, entry = one `<script type="module">`):** `presets.js` (data + clampNumber) · `spec-builders.js` (pure DOM-free spec builders — the correctness core) · `guides.js` (pure hash routing) · `api.js` (fetch wrapper, same-origin, ApiError normalization) · `session.js` (me() bootstrap + focus + 5-min poll, change events) · `auth-ui.js` (modal + header chip) · `paywall.js` (402 modal) · `exporter.js` (flow + idempotency + drawer + toast) · `editor.js` (element state machine) · `bin-tool.js` / `whatnot-tool.js` / `fnsku-tool.js` (DOM collection + validation) · `app.js` (switchMode, routing, `window.LN` namespace, init). **`spec-builders.js` is a deliberate 14th-file split out of exporter.js** so the builders import into node tests with zero DOM. All ~30 inline handlers now call `LN.*` (kept inline on purpose — full listener migration is a later polish brick). index.html: 1191 → 636 lines (74.1KB → 56.9KB).
- **Spec-builder contracts (mirror the legacy DOM layout math):** bin — one page/label, portrait justify-around / landscape justify-center-gap-0.28in spacing formulas, title in*72 → pt, landscape title shrink (88% width / 0.56·len fit), barcode value = bin text with `\s+`→`-`, `show_text` from the checkbox. whatnot — fitted font = min(0.72·h, 0.9·w/(0.58·len)), vertically centered, one text/page. fnsku — fixed 2x1 page at the old 320x180px canvas scale (14px→6.3pt title, 65px→0.406in barcode, 12px→5.4pt condition, justify-around over 3 items). editor — x_in = x/width_px·printWidth_in, pt = px·printWidth_in·72/width_px; text keeps align within remaining width; badge → black rect + white text; box → stroked rect + text; image → dataURL prefix-stripped `data_base64`; webp src throws `webp_image` (conversion is the caller's job). All coords clamped to page, rounded 4dp.
- **Page-cap UX:** server caps 200 pages/spec but the bin tool allowed 1–500 and whatnot 1–999 — both tools now validate client-side and tell the user to split the batch (alert, existing UX pattern). No server change.
- **Idempotency-key lifecycle:** key = `ln-<crypto.randomUUID()>` generated at first Download click for the current tool state; a `dirty` flag set by any input/change/drag/template/preset mutation (delegated per-mode container listeners + editor hooks) regenerates the key on the NEXT click. Same key → server replays the same completed job (proven: same id, remaining unchanged). 402 marks dirty so the post-upgrade retry gets a clean key. Double-click while in flight is also covered (same key, server replay; button disabled during flight).
- **Pending-export-after-auth:** signed-out users can build anything; clicking Download stores `{tool, buildBody, button}` and opens the auth modal ("Create a free account to export — you get 10 free exports"). Successful register/login → `refreshSession()` (register/login responses lack free_uses; /me is authoritative) → the SAME export runs automatically — one intent, one click after auth. Callback direction is exporter→auth-ui (`setAfterAuth`) so there's no import cycle.
- **WebP→PNG conversion at upload time** (not export time): `image/webp` files are drawn to a canvas and re-encoded PNG before ever becoming an element — preview and spec share the converted dataURL, so the builder never sees webp (and the server's 400 can't happen). 8MB per-file cap unchanged.
- **Auth/paywall/drawer UI:** header = usage chip (`9 of 10 free` / `PRO`) + Sign in | email + Sign out + My Exports (auth-gated). Paywall on 402: exact title "You've used your 10 free exports", "work is saved as-is" copy, benefits list, price area from `/api/config/pricing` (today `configured:false` → "Pro pricing coming soon — early access" + mailto notify), Upgrade → `/pricing`, "Back to my work" default focus. Modal never touches tool state. Exports drawer: history (`GET /api/exports?limit=50` — list responses omit `download_url`, client constructs `/api/export/:id/download`), re-download → blob save, delete with confirm, 7-day expiry shown per row.
- **New routes over the SPA:** `/pricing` (worker not invoked — asset/SPA fallback serves the app; `app.js` routes it to guides + `#pricing` section, which also anchors from the paywall) and `/reset?token=` (opens the auth modal directly in set-new-password mode — small addition beyond the brief so reset emails don't dead-end; request-side stays generic "check your email"). `#pricing` joined the guide hash family (7 guide hashes now). Two test-print buttons (4x6, 2x1) live in the guides "settings that usually work" box — the test_print seam needed a UI or it was dead code.
- **Verification:** `scripts/test-spec-builders.mjs` 61/61 (bin A1–A3 exact dims/coords/values, whatnot #1–#2 tiny stock, fnsku sample, editor text+barcode+box+png mapping + px→pt exactness, webp/gif guards, test_print settings, 7 guide hashes + 3 tool hashes routing). `scripts/test-b3-integration.mjs` 41/41 vs wrangler dev: all 13 module URLs 200, register→builder-spec export→download `%PDF`→verify-pdf **288.00×432.00pt ×3 pages**, idempotent double-click (same job id, remaining 9→9), signed-out 401 (modal trigger), fresh-user burn 10→0 then 11th **402 `free_limit_reached` + `upgrade_url:"/pricing"`**. Headless Chrome DOM proof: module graph executes (JS-populated whatnot hint + editor template render; caught+fixed a missing `#auth-to-signin` link found exactly this way), hash routing live (`#tools/warehouse-rack-bin-label-generator` → bin visible, editor hidden), zero console errors; screenshots `scratch/b3-home.png` (1440×900), `b3-mobile.png` (390×844), `b3-bin.png`.
- **Release:** Worker `b639a3d9-c7cb-406f-8a91-bea4fa7af5dc` (label-ninja.com + www) + Pages mirror `f3f75af2.label-ninja.pages.dev`. Prod evidence: `/` 200 with module tag, all 13 module URLs 200, `/api/health` 200, static guides (`/privacy.html`, `/sitemap.xml`, `/robots.txt`) 200, 7 guide hashes 200, canary `ln-canary-b3+20260901020056@bisket.com` register→ONE export→download→verify-pdf 288.00×432.00pt ×3.

## 2026-08-31: Brick 4 — Stripe billing backend + canceled-paid-through entitlement fix

- **Entitlement semantics fix (correctness bug in live code):** spec rule "access remains through the paid-through date after normal cancellation" is now real. `src/entitlements.js` `isProActive` = `status active|trialing` → pro; `canceled|past_due` → pro only while `paid_through > now`; `incomplete`/`unpaid`/null never grant. The `plan === 'pro'` term was **dropped from the predicate** — `plan` is now a synced display column derived FROM the predicate (billing's `applySubscriptionState` imports `isProActive` to set it; reading it back would be circular). No Stripe-key dependency: predicate is pure status+paid_through. B2 bonus path (plan='pro' + status='active' via d1) still passes.
- **`paid_through` units:** Stripe `current_period_end` is unix SECONDS; the column stores MILLISECONDS (consistent with every other timestamp here). `applySubscriptionState` converts `×1000`. Handles both `sub.current_period_end` and `items[0].current_period_end` (newer API versions).
- **`src/billing.js` (370 lines, Stripe via REST fetch — no SDK, no new deps):** `GET /api/config/pricing` (module-level per-isolate `Map` price cache, `expand[]=product`; unkeyed → `{"ok":true,"configured":false}`; keyed-but-fetch-fails → `configured:false, error:"price_fetch_failed"`; keyed-but-price-ids-missing → `error:"prices_not_configured"` — real Stripe data only, never invented strings), `POST /api/billing/checkout` (401→503→400 invalid_plan guards; ensure-customer with `metadata[user_id]`; success `/billing?checkout=success`, cancel `/pricing`; upstream failure → 502 `stripe_upstream_error`), `POST /api/billing/portal` (400 `no_customer` without a bound Stripe customer). Non-2xx Stripe responses throw `{status, stripeCode}`; logs carry the error CODE only, never the body (account data).
- **Webhooks (`POST /api/webhooks/stripe`):** manual Stripe-scheme verification — parse `t` + every `v1`, `|now−t| ≤ 300s`, HMAC-SHA256(secret, `${t}.${body}`) hex, constant-time compare (reuses db.js `timingSafeEqual`); any failure → 400 `invalid_signature`. Missing `STRIPE_WEBHOOK_SECRET` also 400s (nothing can verify — safe by default). Idempotency: `INSERT OR IGNORE INTO webhook_events` claims the event id; redelivery → `{"ok":true,"duplicate":true}` and STOP — **refinement:** if the stored result starts with `error:`, the claim is released and the event reprocesses (otherwise Stripe's retries would short-circuit as duplicates and an errored event could never land; spec's "return 500 so Stripe retries" only works with this release-on-error). Handler error → `result='error: <code>'` + HTTP 500 so Stripe retries. Success stores `result` and returns it in the 200 body.
- **Handlers:** `checkout.session.completed` binds `stripe_customer_id` when null; if the user already has a DIFFERENT customer → `customer_mismatch_ignored` and NO relink (anti-hijack); subscription fetch failure degrades to `customer_bound_subscription_fetch_deferred` 200 (the accompanying `customer.subscription.*` event always carries the state — failing delivery over the fetch would cause a pointless retry storm). `customer.subscription.updated|deleted` resolves user by `stripe_subscription_id`, else by customer when exactly one user is bound, else `no_matching_user`. `invoice.payment_failed` sets `past_due` (access continues via paid_through) and re-syncs plan via the predicate. Unknown types → `ignored`.
- **`applySubscriptionState` = one transition:** sets `stripe_subscription_id`, `subscription_status`, `paid_through` (ms), and `plan = isProActive(...)` — the SAME predicate that gates access, so plan can never disagree with actual entitlements.
- **Env contract:** `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_MONTHLY`, `STRIPE_PRICE_ANNUAL` as Worker secrets only (never wrangler.toml). `.dev.vars.example` committed; `.dev.vars` git-ignored (local fakes `sk_test_fake_local` / `whsec_fake_local_secret` / `price_fake_*`).
- **Verification:** `scripts/test-billing-local.ps1` — Phase A (`-NoKey`, run with `.dev.vars` temporarily renamed): 3/3 (pricing unconfigured exact body; checkout 401; webhook 400). Phase B: 31/31 — pricing degrade, checkout/portal guards, and the signed-webhook state machine proven end-to-end with locally HMAC-crafted events (active→pro; duplicate id no-op; **canceled+15d keeps unlimited:true** (THE spec rule); canceled−1d → free, consumed intact; past_due+future keeps access; tampered v1 → 400; stale t−3600 → 400; mismatched customer ignored with row proven via d1; unknown user `no_matching_user`), export regression (free consumes / pro doesn't, ledger count unchanged), predicate unit sanity incl. B2 bonus path. Auth 24/24, export 27/27 — no regressions. Test-script gotchas fixed en route: PS 5.1 parses `NowUnix + 3600` as command ARGUMENTS not arithmetic (delta-parameter pattern required), and reruns need run-unique event ids + Stripe object ids because `webhook_events` and `stripe_subscription_id` lookups persist in local D1.
- **Release:** Worker version `5e799b62-7c72-4fb8-bce5-4e421fd5b30a` (label-ninja.com + www). Prod evidence: health 200, pricing `{"ok":true,"configured":false}` (no secrets yet — correct), unsigned webhook 400 `invalid_signature`, checkout unauth 401. Billing is live but UNCONFIGURED — the only remaining external step is Chris's 4 `wrangler secret put` + Stripe dashboard setup (SYSTEM_REFERENCE.md has the exact list).
- **No migrations:** schema v1 already carried `users.stripe_*` columns and `webhook_events` (forward-built in brick 1).

## 2026-08-31: SaaS pivot brick 2 — server-side PDF export engine + atomic 10-use metering

- **Reservation-then-generate pattern:** `POST /api/export` enforces the free entitlement with a single atomic statement — `INSERT INTO usage_ledger ... SELECT ... WHERE (granted + admin deltas − export count) > 0`. `stmt.changes === 0` → 402 `free_limit_reached` with `upgrade_url:"/pricing"` inside the error object; the client's spec is never deleted. Pro-active users (plan/subscription_status/paid_through via `src/entitlements.js`) skip reservation entirely (no ledger row, `uses_consumed=0`, `remaining_free_uses:null`). Entitlement math extracted from auth.js into `src/entitlements.js` — one formula, two consumers.
- **Compensation on failure:** any render/store error after reservation deletes the ledger row and marks the job `failed` — a failed export consumes zero uses (proven: corrupt-PNG payload → 500 `export_failed`, `/me` unchanged). Logs carry job id + error class only; `settings_json` is never stored or logged (it can contain user label text).
- **D1 chunk storage (R2 unavailable, error 10042):** completed PDFs live in `output_chunks` (`migrations/0002_outputs.sql`, composite PK `job_id, seq`) as ≤400KB BLOBs, ≤96 chunks/output. Read path concatenates `ORDER BY seq` into a `Uint8Array`. 7-day expiry with a lazy bounded sweep (50 jobs/pass, runs on POST + history GET); DELETE drops chunks immediately. Downloads: `Content-Disposition: attachment`, `Cache-Control: private, no-store`.
- **Renderer (`src/render/pdf-label.js` + `src/code128.js`):** pure pdf-lib vector output — exact `setSize(w_in*72, h_in*72)` pages, Helvetica/Bold text with WinAnsi sanitization (unsupported glyphs → `?`, avoids pdf-lib encode throws), top-down y with 0.75×ascent conversion, multi-line at 1.2×, and a from-scratch CODE128 encoder (107-pattern table + mod-103 checksum, bars as rects; verified against spec vectors: `A` → start `211214`, check `131123`, stop `2331112`). `tool='test_print'` is a real product feature: server-generated diagnostic page (border, corner L-marks, 1-inch ruler with 1/8in ticks, orientation text, dated, CODE128 `TEST-12345`).
- **Limits (`src/limits.js`, all pre-consumption):** 200 pages, 200 elements/page, 2000 chars/text, 20 images/page, 8MB total + 4MB/image base64, 10MB body, coords/dims ≤100in, barcode values printable-ASCII ≤200 (400 `invalid_barcode_value`), images PNG/JPEG magic-checked (400 `invalid_image`/`unsupported_image_format`; WebP rejected — client converts), `pdf_convert` → 501 `not_implemented_yet` (Brick 3). Rate limit 30/hour/user (`export:<uid>` bucket, reuses `rate_limits` table).
- **Idempotency:** unique `(user_id, idempotency_key)` replay returns the same completed job body with no new use; failed/processing/expired rows under the same key are deleted (with any orphan ledger row) and retried fresh.
- **Endpoints:** `POST /api/export`, `GET /api/exports?limit=`, `GET /api/export/:id`, `GET /api/export/:id/download`, `DELETE /api/export/:id` — all auth-required; foreign job ids 404 (never 403, no existence leak). Wired in `src/worker.js` (one prefix dispatch, router still thin). `package.json` → `"type":"module"` (src is ESM; enables node-side verify scripts to import it).
- **Verification:** `scripts/test-export-local.ps1` — 13/13 cases (fresh user 10 remaining; 3-page export → consumed 1; idempotent replay same id; exhaustion 10+402; history/delete/410; `%PDF` + headers; dimension proofs **288.00×432.00pt (4×6in)** and **70.87×36.85pt (25×13mm)** via `scripts/verify-pdf.mjs`; corrupt-image compensation; test_print; ownership 404; parallel race exactly one 200 + one 402; 31st-in-hour 429) + bonus pro-override pass. Auth suite still 24/24. Prod canary `ln-canary-b2+20260831181300@bisket.com`: one export, `/me` remaining 9, idempotent replay same id, download dimension-proofed. Production exhaustion math NOT exercised (deliberate — proven locally).
- **Release:** Worker version `d73cb865-5ca4-4c78-9a02-a2ed39495df8`, migration 0002 applied local+remote.

## 2026-08-31: SaaS pivot brick 1 — Worker API + D1 auth

- **Decision:** Add a plain-ESM Worker API at `/api/*` on the same domain (Worker runs only when no static asset matches; assets block + custom domains unchanged). D1 database `label-ninja-db` (id `852d3ccd-83b6-4ab9-9c39-49a1cf77b88b`) bound as `DB`; migration `0001_init.sql` applied local+remote with all forward tables (export_jobs, usage_ledger, projects, printer_profiles, webhook_events) so future bricks never re-shape v1.
- **Auth design:** PBKDF2-SHA256 (100k iters, 16-byte salt) passwords; 32-byte-hex session tokens in HttpOnly+Secure+SameSite=Lax cookie `ln_session` (30d), stored as SHA-256(token) in `sessions`; logout deletes; password reset via 48-byte-hex tokens (1h, single-use, revoke all sessions) "sent" through pluggable mailer (Resend when `RESEND_API_KEY` set, else `[RESET-LINK]` console.log read via `wrangler tail`). Login burns dummy PBKDF2 on unknown email to kill timing enumeration.
- **Rate limits:** fixed-window counter in D1 `rate_limits` — 10/hour per IP across register/login/reset endpoints; 429 + `Retry-After`.
- **ADMIN_EMAILS bootstrap:** `[vars] ADMIN_EMAILS="chrislucas252@gmail.com"` → matching registrants get role `admin`.
- **R2:** bucket creation failed (`code 10042` — R2 not enabled on the account). No `OUTPUTS` binding; Brick 2 output storage falls back to D1 blobs or needs R2 enabled in the dashboard first.
- **Endpoints:** `GET /api/health`, `POST /api/auth/{register,login,logout,reset-request,reset-confirm}`, `GET /api/auth/me` (free-uses math: granted + admin adjustments − export count; `unlimited:true` when pro+active). Verified 24/24 local checks (`scripts/test-auth-local.ps1`) and full curl pass in production.
- **Release:** commit `5b8611e`, Worker version `0e4d7460-db61-4bac-8be9-2a183d51e2d6`, canary account `ln-canary+20260831175445@bisket.com` registered in prod.

## 2026-08-31: Ads off — Adsterra removed from operational pages

- **Decision:** Remove all Adsterra ad units (leaderboard + rectangles) from `index.html` and `privacy.html`, set `ADS.enabled: false` in `js/ads-config.js`, and drop the `ads.js` module import and AdSense `adsbygoogle.js` loader from both pages. The integration (`js/ads.js` + `js/ads-config.js`) stays in the repo behind the flag; `ads.txt` stays.
- **Reason:** SaaS pivot — ads damage trust in the conversion workflow. Possible future reuse limited to public SEO guide pages.
- **Release:** commit `ef33427`, Worker version `9a41b050-7bc3-4946-8ba9-87a7aee5b1e2`, deployed to Worker custom domains + Pages mirror, verified ad-free on all three endpoints. Also committed prior 08-29 SEO fixes (`e906248`: robots.txt, sitemap URLs, guide copy).

## 2026-08-29: Adsterra display ads + privacy page

- **Decision:** Add Adsterra 728x90 and 300x250 display units across Label Ninja, plus a dedicated `/privacy.html` for AdSense qualification.
- **Reason:** Google allows other networks on the same site if the page still follows publisher policy. Popunder, Social Bar, and Smartlink stay off. Ads load in sandboxed srcdoc iframes because Adsterra `invoke.js` uses `document.write`.
- **Placement:** Leaderboard under the header on every view. Rectangle on the editor (below the canvas), each tool, Printer Guides, and the privacy page. Slots in hidden tabs do not load until that tab is shown.

## 2026-08-25: Freemium Pivot & Domain Acquisition

- **Decision:** Chris acquired `label-ninja.com`. Pivot project to 100% Freemium & Unlimited tool.
- **Changes:**
  - Removed all payment gates, LemonSqueezy modals, and MVP mentions.
  - Implemented 100% client-side CSV manifest parsing via PapaParse.
  - Added support for 5 thermal printer label presets and 5 barcode symbology types (CODE128, UPC-A, EAN-13, CODE39, QR Code).
- **Deployment Plan:** Target Cloudflare Pages static edge host for zero hosting costs.

## 2026-08-26: Physical Print Contracts, Local Artwork, and Production Hosting

- **Decision:** Every generator sets an explicit physical CSS page size before printing. Warehouse bins use 4x6 portrait pages; Whatnot stock is selectable and defaults to 1x0.5 landscape.
- **Reason:** Browser viewport sizing and printer-driver defaults produced oversized or clipped labels. Exact CSS inches keep one complete label on each physical sheet when the matching driver paper, zero margins, and 100% scale are selected.
- **Decision:** Custom-editor image uploads remain browser-only. Accept PNG, JPEG, and WebP files up to 8 MB; do not upload or retain user files on a server.
- **Reason:** This preserves privacy, eliminates storage cost, and keeps untrusted files away from a server boundary.
- **Decision:** `label-ninja.com` and `www` are served by Cloudflare Worker Static Assets. `label-ninja.pages.dev` remains a synchronized mirror.
- **Reason:** The custom domains were already attached to the Worker; static-assets mode deploys the canonical `public/` directory directly and removes the stale generated-worker seam.
